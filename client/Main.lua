--[[

	// @local
	// FileName: Main.lua
	// Written by: tgarbrecht
	// Description: Runs the init function to initiate all local scripts on the client.

]]

shared.Utility = {}
require(script.Parent.Init).Init()